# Changelog

## Unreleased

- Reworked the project as a JVM Clojure package using `deps.edn`.
- Added `clojure.test` coverage for workflow, rendering, command execution, locking, and BigTofu helpers.
- Switched Selmer rendering from a local checkout to the published Maven artifact `selmer 1.13.1`.
