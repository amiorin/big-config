# Run from the repository root:
# cat .github/workflows/ci.yml.local | jet -i yaml -o json | jq -r .test | bash -s

test: >-
  bash -lc 'set -euo pipefail; test -d /home/ubuntu/code/bigconfig/Selmer/python; uv sync --frozen; uv run pytest -q'
